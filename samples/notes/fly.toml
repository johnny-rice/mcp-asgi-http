# Deploy from repo root:
#   fly apps create mcp-asgi-http-notes
#   fly secrets set MCP_API_KEY=... SITE_URL=https://mcp-asgi-http-notes.fly.dev
#   fly deploy -c samples/notes/fly.toml

app = "mcp-asgi-http-notes"
primary_region = "iad"

[build]
  dockerfile = "samples/notes/Dockerfile"

[env]
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[http_service.checks]]
  interval = "15s"
  timeout = "2s"
  grace_period = "5s"
  method = "GET"
  path = "/health"
