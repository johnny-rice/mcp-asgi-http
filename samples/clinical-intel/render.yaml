services:
  - type: web
    name: mcp-asgi-http-clinical
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ../..
    plan: free
    healthCheckPath: /health
    envVars:
      - key: MCP_API_KEY
        generateValue: true
      - key: SITE_URL
        sync: false
