{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"mcp-asgi-http","text":"<p>Mount an MCP server on an ASGI app over Streamable HTTP. Optional request auth. Works when ASGI lifespan is off (Mangum on Lambda).</p> <pre><code>pip install mcp-asgi-http\n</code></pre> <pre><code>from fastapi import FastAPI\nfrom mcp_asgi_http import ApiKeyAuth, mount_mcp\n\napi = FastAPI(redirect_slashes=False)\napp = mount_mcp(api, server, auth=ApiKeyAuth(allowed_keys={\"dev-secret\"}))\n</code></pre>"},{"location":"#where-to-go","title":"Where to go","text":"<ul> <li>Quickstart</li> <li>Auth</li> <li>Serverless</li> <li>Samples (including live demos when deployed)</li> <li>Blog</li> </ul>"},{"location":"#source","title":"Source","text":"<p>github.com/johnny-rice/mcp-asgi-http</p>"},{"location":"related/","title":"Related work","text":"Project Difference FastMCP Tool registration / DX. This package mounts and authenticates HTTP. AWS Serverless MCP Server MCP tools for SAM/Lambda development, not an ASGI mount library. invariantlabs-ai/mcp-streamable-http Transport examples. <code>mcp-streamablehttp-proxy</code> / <code>mcp-streamablehttp-client</code> stdio \u2194 HTTP bridges. CodeSignal: mount MCP in FastAPI Intro to <code>app.mount</code> + SSE. This package targets Streamable HTTP, auth, and Mangum. <p>Official transport: Streamable HTTP.</p>"},{"location":"blog/","title":"Blog","text":"<p>Notes on mounting remote MCP, Streamable HTTP, and serverless ASGI.</p>"},{"location":"blog/2026/07/23/why-mcp-asgi-http-exists/","title":"Why mcp-asgi-http exists","text":"<p>The official MCP Python SDK and FastMCP can expose Streamable HTTP. Tutorials show <code>app.mount(\"/mcp\", ...)</code>. That is enough for local demos.</p> <p>Problems show up when you put the same pattern on Mangum with <code>lifespan=\"off\"</code>, share auth with a REST API, or hit Function URL slash redirects. This package is the small set of helpers we kept rewriting for that case:</p> <ol> <li>Stateless Streamable HTTP ASGI app (JSON responses, no session store)</li> <li>Path middleware instead of <code>Mount</code> for <code>/mcp</code></li> <li>Optional API key / JWT / dual auth, or <code>auth=None</code> if the gateway already authenticated</li> <li><code>once_ready</code> for lazy init when lifespan never runs</li> </ol> <p>It is not a tool framework. Register tools with the official SDK (or FastMCP). Use this to mount and protect <code>/mcp</code> next to your existing API.</p>"},{"location":"blog/2026/07/23/why-mcp-asgi-http-exists/#what-this-is-not","title":"What this is not","text":"<ul> <li>Not AWS Serverless MCP Server (that product is guidance tools for SAM/Lambda).</li> <li>Not an SSE mount tutorial. Prefer Streamable HTTP for new remote servers.</li> <li>Not a stdio proxy. See <code>mcp-streamablehttp-proxy</code> if you need that bridge.</li> </ul>"},{"location":"blog/2026/07/23/why-mcp-asgi-http-exists/#try-it","title":"Try it","text":"<pre><code>pip install mcp-asgi-http\n</code></pre> <p>Or run a fullstack sample and point Cursor / Claude at the public <code>/mcp</code> URL.</p>"},{"location":"guide/auth/","title":"Auth","text":"Mode Behavior <code>auth=None</code> No auth in this library. Use API Gateway or your middleware. <code>ApiKeyAuth</code> <code>Authorization: Bearer</code> or <code>X-API-Key</code> <code>JwtAuth</code> Bearer JWT via JWKS <code>DualAuth</code> API key first, then JWT"},{"location":"guide/auth/#api-key","title":"API key","text":"<pre><code>from mcp_asgi_http import ApiKeyAuth, mount_mcp\n\napp = mount_mcp(\n    api,\n    server,\n    auth=ApiKeyAuth(allowed_keys={\"dev-secret\"}),\n)\n</code></pre>"},{"location":"guide/auth/#dual-jwt-api-key","title":"Dual JWT + API key","text":"<pre><code>from mcp_asgi_http import build_auth_backend, mount_mcp\n\nauth = build_auth_backend(\n    \"dual\",\n    api_keys={\"dev-secret\"},\n    jwt_issuer=\"https://auth.example.com/oauth2/default\",\n    jwt_audience=\"api://demo\",\n    jwt_jwks_url=\"https://auth.example.com/oauth2/default/v1/keys\",  # optional\n)\napp = mount_mcp(api, server, auth=auth)\n</code></pre> <p>If <code>jwt_jwks_url</code> is omitted, the default is <code>{issuer}/.well-known/jwks.json</code>.</p>"},{"location":"guide/auth/#auth-outside-this-library","title":"Auth outside this library","text":"<p>Authenticate in a gateway or middleware, then pass <code>auth=None</code>:</p> <pre><code>app = mount_mcp(api, server, auth=None)\n</code></pre> <p>See <code>examples/04_consumer_owned_auth/</code> in the repo.</p>"},{"location":"guide/enrich-playbook/","title":"Enrich and playbook","text":""},{"location":"guide/enrich-playbook/#enrich","title":"Enrich","text":"<p>Mutate tool JSON before returning it (for example add deep links):</p> <pre><code>from mcp_asgi_http import tool_result_contents\n\ndef add_href(name: str, data: object) -&gt; object:\n    if name == \"get_item\" and isinstance(data, dict):\n        data = dict(data)\n        data[\"href\"] = f\"https://app.example.com/items/{data['id']}\"\n    return data\n\ncontents = tool_result_contents(\"get_item\", {\"id\": 7}, hooks=[add_href])\n</code></pre>"},{"location":"guide/enrich-playbook/#playbook","title":"Playbook","text":"<p>Ship a markdown agent guide as an MCP resource and as a plain HTTP file:</p> <pre><code>from mcp_asgi_http import PlaybookSpec, playbook_http_route, playbook_resource\n\nspec = PlaybookSpec(markdown=\"# Rules\\n\\n1. Cite ids.\\n\")\n# list_resources / read_resource -&gt; playbook_resource(spec)\napi.routes.append(playbook_http_route(spec))  # GET /agent-guide.md\n</code></pre> <p>See <code>examples/06_enrich_and_playbook/</code> and the Catalog sample.</p>"},{"location":"guide/install/","title":"Install","text":"<pre><code>pip install mcp-asgi-http\n</code></pre> <p>JWT backends need the extra:</p> <pre><code>pip install 'mcp-asgi-http[jwt]'\n</code></pre> <p>Requires Python 3.10+.</p> <p>From a clone:</p> <pre><code>pip install -e '.[dev]'\npytest -q\n</code></pre>"},{"location":"guide/quickstart/","title":"Quickstart","text":"<p>Register tools on an official MCP <code>Server</code>, then mount it on FastAPI (or any ASGI app).</p> <pre><code>from typing import Any\n\nimport mcp.types as types\nfrom fastapi import FastAPI\nfrom mcp.server import Server\nfrom mcp_asgi_http import mount_mcp\n\nserver = Server(\"demo\")\n\n\n@server.list_tools()\nasync def list_tools() -&gt; list[types.Tool]:\n    return [\n        types.Tool(\n            name=\"echo\",\n            description=\"Echo a message\",\n            inputSchema={\n                \"type\": \"object\",\n                \"properties\": {\"message\": {\"type\": \"string\"}},\n                \"required\": [\"message\"],\n            },\n        )\n    ]\n\n\n@server.call_tool()\nasync def call_tool(name: str, arguments: dict[str, Any]) -&gt; list[types.TextContent]:\n    return [types.TextContent(type=\"text\", text=str(arguments.get(\"message\", \"\")))]\n\n\napi = FastAPI(redirect_slashes=False)\n\n\n@api.get(\"/health\")\ndef health() -&gt; dict[str, str]:\n    return {\"status\": \"ok\"}\n\n\napp = mount_mcp(api, server, auth=None)\n</code></pre> <p>Run with uvicorn:</p> <pre><code>uvicorn main:app --host 127.0.0.1 --port 8000\n</code></pre> <p>MCP endpoint: <code>http://127.0.0.1:8000/mcp</code></p> <p>Prefer <code>mount_mcp</code> over Starlette <code>Mount</code> under Mangum / Function URL. See Serverless.</p>"},{"location":"guide/serverless/","title":"Serverless","text":"<p>When Mangum runs with <code>lifespan=\"off\"</code>, FastAPI lifespan never runs. Use <code>once_ready</code> for lazy init (DB pool, caches).</p> <pre><code>from mangum import Mangum\nfrom mcp_asgi_http import ApiKeyAuth, mount_mcp, once_ready\n\nasync def warm() -&gt; None:\n    # open pool, warm caches, etc.\n    ...\n\napp = mount_mcp(\n    api,\n    server,\n    auth=ApiKeyAuth(allowed_keys={\"lambda-dev-key\"}),\n    on_ready=once_ready(warm),\n)\nhandler = Mangum(app, lifespan=\"off\")\n</code></pre> <p>Also set <code>redirect_slashes=False</code> on FastAPI. <code>mount_mcp</code> uses path middleware so <code>/mcp</code> does not hit Mount slash-redirect loops on Function URL.</p> <p>See <code>examples/05_mangum_lambda/</code> and the Notes sample Docker image.</p>"},{"location":"samples/","title":"Samples","text":"<p>Runnable apps under <code>samples/</code> in the repo. Each has a Dockerfile so you can publish a live instance.</p> Sample What it shows Live Notes FastAPI + MCP + small web UI, API key auth See sample README for URL after deploy Catalog Enrich hooks, agent playbook, public guide page See sample README for URL after deploy <p>Short snippets without a UI live under <code>examples/</code>.</p>"},{"location":"samples/#deploy-a-live-demo","title":"Deploy a live demo","text":"<p>Each sample includes:</p> <ul> <li><code>Dockerfile</code></li> <li><code>render.yaml</code> (Render)</li> <li><code>fly.toml</code> (Fly.io)</li> </ul> <p>Pick one host, set <code>MCP_API_KEY</code> (and any other env vars), deploy, then paste the public <code>/mcp</code> URL into your MCP client.</p>"},{"location":"samples/catalog/","title":"Catalog demo","text":"<p>Source: <code>samples/catalog/</code></p> <ul> <li>Catalog UI</li> <li>Open MCP (<code>auth=None</code> for the public demo)</li> <li>Enrich hooks add <code>href</code> when <code>SITE_URL</code> is set</li> <li>Agent guide: MCP resource + <code>GET /agent-guide.md</code></li> </ul>"},{"location":"samples/catalog/#run-locally","title":"Run locally","text":"<pre><code>pip install -e '.[dev]'\ncd samples/catalog\nSITE_URL=http://127.0.0.1:8081 PYTHONPATH=../../src \\\n  uvicorn app:app --port 8081\n</code></pre>"},{"location":"samples/catalog/#live-instance","title":"Live instance","text":"App (set after deploy) MCP <code>https://&lt;host&gt;/mcp</code> Guide <code>https://&lt;host&gt;/agent-guide.md</code>"},{"location":"samples/notes/","title":"Notes demo","text":"<p>Source: <code>samples/notes/</code></p> <ul> <li>Web UI for notes</li> <li>REST under <code>/api/notes</code></li> <li>MCP at <code>/mcp</code> with API key auth (<code>MCP_API_KEY</code>)</li> <li><code>once_ready</code> seed data</li> </ul>"},{"location":"samples/notes/#run-locally","title":"Run locally","text":"<pre><code>pip install -e '.[dev]'\ncd samples/notes\nMCP_API_KEY=demo-secret SITE_URL=http://127.0.0.1:8080 \\\n  PYTHONPATH=../../src uvicorn app:app --port 8080\n</code></pre>"},{"location":"samples/notes/#live-instance","title":"Live instance","text":"<p>Deploy with Docker + Fly or Render (configs in the sample folder). After deploy, record the URL:</p> App (set after deploy) MCP <code>https://&lt;host&gt;/mcp</code> Auth <code>X-API-Key: &lt;MCP_API_KEY&gt;</code>"},{"location":"blog/archive/2026/","title":"2026","text":""},{"location":"blog/category/mcp/","title":"mcp","text":""},{"location":"blog/category/asgi/","title":"asgi","text":""}]}